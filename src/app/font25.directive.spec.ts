import { Font25Directive } from './font25.directive';

describe('Font25Directive', () => {
  it('should create an instance', () => {
    const directive = new Font25Directive();
    expect(directive).toBeTruthy();
  });
});
